# Нереализованный функционал по сравнению с бэкендом

## ✅ Реализовано

### Авторизация
- ✅ Регистрация (POST /api/auth/register)
- ✅ Подтверждение email (POST /api/auth/verify-email)
- ✅ Повторная отправка кода (POST /api/auth/resend-code)
- ✅ Вход (POST /api/auth/login)
- ✅ Выход (POST /api/auth/logout)
- ✅ Восстановление пароля (POST /api/auth/forgot-password, POST /api/auth/reset-password)
- ✅ OAuth Яндекс (GET /api/auth/yandex, GET /api/auth/yandex/callback)
- ✅ OAuth VK (частично)

### Пользователь
- ✅ Получение профиля (GET /api/user/profile)
- ✅ Обновление профиля (PUT /api/user/profile) - частично (не все поля)
- ✅ Загрузка изображений (POST /api/upload/image)

### События
- ✅ Получение списка событий (GET /api/events) - с фильтрацией по tab
- ✅ Получение события по ID (GET /api/events/:id)
- ✅ Подтверждение участия (POST /api/events/:id/join)
- ✅ Отмена участия (DELETE /api/events/:id/leave)
- ✅ Создание события (POST /api/events) - API готов, но не интегрирован в UI
- ✅ Обновление события (PUT /api/events/:id) - API готов, но не интегрирован в UI
- ✅ Удаление события (DELETE /api/events/:id) - API готов, но не интегрирован в UI

## ❌ НЕ реализовано

### События
- ❌ Полная фильтрация событий:
  - ❌ Поиск по названию и описанию (параметр `search`)
  - ❌ Фильтр по категориям (`categoryIDs`)
  - ❌ Фильтр по тегам (`tags`)
  - ❌ Фильтр по датам (`dateFrom`, `dateTo`)
  - ❌ Сортировка (`sortBy`, `sortOrder`)
  - ❌ Пагинация (`page`, `limit`)
- ❌ Экспорт участников события (GET /api/events/:id/export) - CSV/XLSX
- ❌ Интеграция создания/редактирования событий в AdminPage

### Отзывы о событиях
- ❌ Получение отзывов (GET /api/events/:id/reviews)
- ❌ Создание отзыва (POST /api/events/:id/reviews)
- ❌ Обновление отзыва (PUT /api/events/:id/reviews/:reviewId)
- ❌ Удаление отзыва (DELETE /api/events/:id/reviews/:reviewId)
- ❌ Отображение рейтинга и отзывов на странице события

### Геокодинг и карты
- ❌ Геокодинг адреса в координаты (POST /api/geocoder/geocode)
- ❌ Обратный геокодинг координат в адрес (POST /api/geocoder/reverse)
- ❌ Генерация ссылки на Яндекс.Карты (POST /api/geocoder/map-link)

### Администрирование

#### Управление пользователями
- ❌ Получение списка пользователей (GET /api/admin/users) - с фильтрацией и пагинацией
- ❌ Получение пользователя по ID (GET /api/admin/users/:id)
- ❌ Создание пользователя (POST /api/admin/users)
- ❌ Обновление пользователя (PUT /api/admin/users/:id)
- ❌ Сброс пароля пользователя (POST /api/admin/users/:id/reset-password)
- ❌ Удаление пользователя (DELETE /api/admin/users/:id)
- ❌ Интеграция всех функций в AdminPage (сейчас только фейковые данные)

#### Управление событиями
- ❌ Получение всех событий для админа (GET /api/admin/events) - включая отклоненные
- ❌ Интеграция создания/редактирования/удаления событий в AdminPage

### Категории событий
- ❌ Получение списка категорий (GET /api/categories)
- ❌ Создание категории (POST /api/admin/categories)
- ❌ Обновление категории (PUT /api/admin/categories/:id)
- ❌ Удаление категории (DELETE /api/admin/categories/:id)
- ❌ Выбор категорий при создании/редактировании события

### Интересы
- ❌ Получение списка интересов (GET /api/interests)
- ❌ Интеграция интересов в профиль пользователя

### Сообщества
- ❌ Получение списка сообществ (GET /api/communities)
- ❌ Создание/управление сообществами

### Профиль пользователя
- ❌ Обновление всех полей профиля:
  - ❌ `telegram` (сейчас только `fullName` и `avatar`)
  - ❌ `phone`, `birthDate` (если поддерживается бэкендом)
- ❌ Управление интересами пользователя

### Дополнительные функции
- ❌ Health check (GET /health) - не критично
- ❌ Обработка ошибок API с правильными сообщениями
- ❌ Загрузка изображений при создании события
- ❌ Валидация форм на фронтенде согласно требованиям бэкенда

## 🔧 Требует доработки

### EventsPage
- ⚠️ Переключение вкладок должно делать новый запрос к API (сейчас данные кешируются)
- ⚠️ Фильтрация по дате должна работать на клиенте или через API параметры
- ⚠️ Нет обработки пагинации

### EventDetailsPage
- ⚠️ Нет отображения категорий и тегов
- ⚠️ Нет отображения организатора
- ⚠️ Нет отображения рейтинга и отзывов
- ⚠️ Нет ссылки на Яндекс.Карты

### AdminPage
- ⚠️ Все данные фейковые, нужно заменить на реальные API запросы
- ⚠️ Нет пагинации для пользователей и событий
- ⚠️ Нет обработки ошибок API
- ⚠️ Форма создания события не отправляет данные на сервер

### Профиль
- ⚠️ При ошибке API возвращаются фейковые данные (нужно показывать ошибку)
- ⚠️ Не все поля профиля можно редактировать

## 📝 Примечания

1. **API запросы созданы**, но не все интегрированы в UI
2. **Типы данных** частично не совпадают между фронтендом и бэкендом (нужна маппинг функция)
3. **Обработка ошибок** минимальная, нужно улучшить
4. **Валидация форм** частично реализована, нужно проверить соответствие требованиям бэкенда

